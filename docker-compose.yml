services:
  procurewise:
    build: .
    ports:
      - "8501:8501"
    environment:
      PROCUREWISE_USE_LLM: "false"
      PROCUREWISE_CASE_DIR: data/runtime
    volumes:
      - ./data/runtime:/app/data/runtime
